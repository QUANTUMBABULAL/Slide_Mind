(() => {
  const SlideMind = (window.SlideMind = window.SlideMind || {});

  SlideMind.constants = {
    PANEL_WIDTH_PX: "320px"
  };

  console.info("[SlideMind] loaded file utils/constants.js");
})();
