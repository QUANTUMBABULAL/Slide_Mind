(() => {
  const SlideMind = (window.SlideMind = window.SlideMind || {});
  SlideMind.utils = SlideMind.utils || {};

  SlideMind.utils.captureSlideImage = async () => {
    // TODO: Capture annotated slide as an image for ChatGPT upload.
    return null;
  };

  console.info("[SlideMind] loaded file utils/imageCapture.js");
})();
